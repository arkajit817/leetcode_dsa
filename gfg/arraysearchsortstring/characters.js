let str = "abcdd";

let strarray = str.split('');

let obj = {};

for(let i = 0; i < strarray.length;i++){
    if(obj[arr[i]]){
        obj[arr[i]] = obj[arr[i]] +1;
    } else {
        obj[arr[i]] = 1;
    }
}